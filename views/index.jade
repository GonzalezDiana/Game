extends layout

block content
  .container
    if (!user)
      a(href="/login") Login
      br
      a(href="/register") Register
    if (user)
      p You registered as: #{user.username}
      br
      a(href="/newgame")
        button.btn.btn-primary(type="button") New Game
      br
      a(href="/logout") Logout
